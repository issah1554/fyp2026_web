import { MlHeader } from "../_components";
import { mlPredictions } from "@/src/services/ml/mlManagementService";

export default function MlPredictionsPage() {
  return (
    <div className="mx-auto flex w-full max-w-7xl flex-col gap-6 py-4">
      <MlHeader title="Predictions" description="Review predictions generated by active models across daily, weekly, monthly, and seasonal horizons." />
      <section className="grid gap-3 rounded-md border border-main-200 bg-main-100 p-4 md:grid-cols-5">
        {["Commodity", "Market", "Prediction Type", "Forecast Period", "Model Version"].map((label) => (
          <label key={label} className="text-sm font-semibold text-main-700">{label}<select className="mt-1 w-full rounded-md border border-main-300 bg-main-0 px-3 py-2"><option>All</option></select></label>
        ))}
      </section>
      <section className="rounded-md border border-main-200 bg-main-100">
        <div className="overflow-x-auto">
          <table className="min-w-full divide-y divide-main-200 text-sm">
            <thead className="bg-main-50 text-left text-xs uppercase text-main-500">
              <tr><th className="px-4 py-3">Type</th><th className="px-4 py-3">Commodity</th><th className="px-4 py-3">Market</th><th className="px-4 py-3 text-right">Observed</th><th className="px-4 py-3 text-right">Predicted</th><th className="px-4 py-3">Period</th><th className="px-4 py-3">Model</th></tr>
            </thead>
            <tbody className="divide-y divide-main-200">
              {mlPredictions.map((prediction) => (
                <tr key={prediction.id} className="hover:bg-main-50">
                  <td className="px-4 py-3">{prediction.type}</td>
                  <td className="px-4 py-3 font-bold">{prediction.commodity}</td>
                  <td className="px-4 py-3">{prediction.market}</td>
                  <td className="px-4 py-3 text-right">{prediction.observedValue.toLocaleString()}</td>
                  <td className="px-4 py-3 text-right font-bold text-primary-700">{prediction.predictedValue.toLocaleString()}</td>
                  <td className="px-4 py-3">{prediction.period}</td>
                  <td className="px-4 py-3">{prediction.modelVersion}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </section>
    </div>
  );
}
